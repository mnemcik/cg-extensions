**Gmail digest shortcut.** When the user asks to *"check X updates"*, *"digest X"*, *"what's new in X"*, *"catch up on X"* — treat it as a **Gmail digest**, not a project task. **Skip project identification only** (it's a recurring reference activity, not project work); the **worktree gate still applies** — create a session worktree first (`cg worktree create digest-<target>-<YYYY-MM-DD>`). Then load [`notes/gmail-digest-workflow.md`](notes/gmail-digest-workflow.md) for the digest process and target-file conventions. Requires a Gmail MCP (or equivalent Gmail access) configured in your client.
